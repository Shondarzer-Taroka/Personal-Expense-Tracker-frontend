import React from 'react';
import ExpenseForm from '../expenses/ExpenseForm';

const page = () => {
    return (
        <div>
            <ExpenseForm/>
        </div>
    );
};

export default page;